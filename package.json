{
  "name": "clipit-chrome-extension",
  "version": "1.0.0",
  "description": "Chrome extension for ClipIt - Smart Web Clipper",
  "scripts": {
    "build": "webpack --mode=production",
    "dev": "webpack --mode=development --watch",
    "clean": "rm -rf dist",
    "package": "npm run build && zip -r clipit-extension.zip dist/"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "copy-webpack-plugin": "^11.0.0",
    "typescript": "^5.0.0",
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.0",
    "ts-loader": "^9.4.0"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  }
}